/* =========================================================================
 * 팀 FCST 집계 - 배포 설정 파일
 * 이 파일만 수정해서 GitHub에 올리면 팀원 전원이 같은 서버를 바라봅니다.
 * (기존처럼 각자 브라우저에서 URL을 따로 입력하지 않습니다)
 * ========================================================================= */
window.FCST_CONFIG = {

  // 1) Apps Script 웹앱 배포 URL (.../exec 로 끝나야 함)
  //    비워두면 '로컬 전용 모드'로 동작하며 팀 공유는 되지 않습니다.
  gasUrl: "",

  // 2) Code.gs 의 APP_TOKEN 과 반드시 동일한 값
  token: "CHANGE_ME_TOKEN",

  // 3) 담당자 목록 (서버 데이터와 무관하게 화면 선택지만 정의)
  owners: ["손창곤", "유슬아", "옥수영", "박명선", "최원준"],

  // 4) 서버 변경 감지 주기(ms). 화면이 보일 때만 동작합니다.
  pollMs: 5000,

  // 5) true 로 두면 '내 이름'으로 선택한 담당자의 행만 수정/삭제할 수 있습니다.
  //    (같은 행을 두 사람이 동시에 만지는 상황 자체를 차단)
  restrictToOwnRow: false
};
